version: 2
jobs:
  build_test:
    docker:
      - image: quay.io/token/go-build:v0.3.7
    working_directory: /home/circleci/github.com/tokencard/contracts
    steps:
      - checkout
      - restore_cache:
          keys:
            - vendor-{{ checksum "go.sum" }}
      - run:
          name: "Setup custom environment variables"
          command: |
            echo 'export GO111MODULE="on"' >> $BASH_ENV # Redirect MY_ENV_VAR into $BASH_ENV
      - run: go mod vendor
      - run:
          command: go run github.com/onsi/ginkgo/ginkgo -r ./test/...
          no_output_timeout: 30m

      - save_cache:
          key: vendor-{{ checksum "go.sum" }}
          paths:
            - "vendor"

  build_check:
    machine: true
    working_directory: /home/circleci/github.com/tokencard/contracts
    steps:
      - checkout
      - restore_cache:
          keys:
            - vendor-{{ checksum "go.sum" }}
      - run: ./build.sh
      - run: '[[ -z $(git status -uno --porcelain) ]] || (git status ; echo "Please run ./build.sh before committing changes!"; exit 1)'

  mythril:
    docker:
      - image: mythril/myth:0.21.19
    working_directory: /src
    steps:
      - checkout
      - run:
          command: myth analyze ./contracts/oracle.sol
          no_output_timeout: 1h
      - run:
          command: myth analyze ./contracts/wallet.sol
          no_output_timeout: 1h
      - run:
          command: myth analyze ./contracts/licence.sol
          no_output_timeout: 1h
      - run:
          command: myth analyze ./contracts/holder.sol
          no_output_timeout: 1h
      - run:
          command: myth analyze ./contracts/tokenWhitelist.sol
          no_output_timeout: 1h
      - run:
          command: myth analyze ./contracts/walletDeployer.sol
          no_output_timeout: 1h

workflows:
  version: 2
  build_deploy:
    jobs:
      - build_test:
          filters:
            tags:
              ignore: ""
      - build_check:
          requires:
            - build_test
          filters:
            tags:
              ignore: ""
      - mythril:
          filters:
            tags:
              ignore: ""
